---
'@getmunin/backend-core': patch
---

The Slack app manifest for self-hosters is now also checked in as `slack-app-manifest.json` at the repo root; the connect-slack skill points to it and a test keeps the two copies in sync.
