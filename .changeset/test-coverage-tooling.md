---
---

chore(ci): test tooling improvements — add `pnpm test:coverage` (aggregated v8 coverage across the monorepo) and label each per-package block in the GitHub Actions test-report summary with its package name instead of a generic "Vitest Test Report" heading.

Tooling/CI only — no published-package surface changes, so this intentionally bumps nothing.
