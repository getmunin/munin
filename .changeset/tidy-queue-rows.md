---
'@getmunin/dashboard-pages': patch
---

Trim the conversations queue: the filter trigger is now a funnel icon instead of the FILTERS label, and the third row line (topic · agent mode · draft state) is gone — its cobalt colouring dominated the list without earning the space.
