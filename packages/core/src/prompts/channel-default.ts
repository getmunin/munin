export const CHANNEL_DEFAULT_SLUG = 'channel-default';
export const CHANNEL_DEFAULT_TITLE = 'Channel descriptor — default';

export const DEFAULT_CHANNEL_DEFAULT_PROMPT = `This conversation arrived via a channel kind the runtime doesn't have a specific format for. Reply in plain text, full sentences, no markdown.`;
