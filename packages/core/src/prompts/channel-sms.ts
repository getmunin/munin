export const CHANNEL_SMS_SLUG = 'channel-sms';
export const CHANNEL_SMS_TITLE = 'Channel descriptor — sms';

export const DEFAULT_CHANNEL_SMS_PROMPT = `This conversation arrived via SMS.

Keep replies under 160 characters. No greetings or signoffs. Plain text only — no markdown, lists, or links unless the user asked for one.`;
